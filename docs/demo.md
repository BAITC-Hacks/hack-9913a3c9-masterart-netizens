# Демонстрация за пять минут

Цель демо — показать вопрос кейса и ответ на него на настоящих данных: живой запуск, затем
разбор двух-трёх счетов по существу, включая счёт, который назовёт жюри.

Подготовка до выступления: файлы организаторов лежат в `data/`, зависимости установлены,
просмотрщик открыт на очереди проверки, терминал — в корне репозитория.

## Сценарий

| Время | Что показываем | Что говорим |
|---|---|---|
| 0:00–0:30 | Заголовок README | Аналитику известен 81 клиент — нижний уровень цепочки. Инструмент восстанавливает сеть из 2 248 счетов и отвечает, кого проверять первым и почему. Каждый вывод — гипотеза для проверки. |
| 0:30–1:10 | Терминал: `python -m backend --data data --out out` | Запуск от сырых parquet до трёх выгрузок меньше чем за секунду. Вход проверяется строже стартового кода: по каждой паре совпадают сумма и число операций. |
| 1:10–2:00 | Просмотрщик: очередь, счёт `100000004015047100` | Плательщики сверху, получатели снизу, стрелки — направление денег. Роль «консолидатор»: 9 разных плательщиков при пороге 7. Рядом — альтернатива «конечный получатель» с почти такой же опорой: мы показываем, почему могли ошибиться. Приоритет разложен на пять признаков. |
| 2:00–2:50 | Поиск: `gid`, который называет жюри | Поиск открывает только точный номер: соседнее число не подменяет счёт. Карточка объясняет роль через метрики и порог правила. Если счёт на 4-м колене, как `100000003037476100`, мы прямо говорим: исходящие не собирались, поэтому это не «конечный получатель». |
| 2:50–3:30 | Режим «позже по датам» | Цепочка от исходного клиента с датой и суммой каждого шага. Порядок дат допускает движение денег, но не доказывает, что двигались те же деньги. Порядок внутри дня неизвестен — для него отдельный режим. |
| 3:30–4:15 | Вкладка «Кластеры», карта кластера | 92 кластера покрывают все счета; ровно 8 содержат несколько исходных клиентов — как в подсказке организаторов. Гипотеза кластера описывает наблюдаемую структуру, а не связь владельцев. |
| 4:15–4:40 | Кнопка справки по счёту | Справка в Markdown для передачи на углублённую проверку: роль, альтернатива, даты, пробелы данных и следующий запрос. |
| 4:40–5:00 | README, разделы 10 и 3 | Ограничения данных учтены явно. При миллионе счетов меняются хранение, кластеризация и загрузка окрестности по запросу. Все функции связаны с тестами в FEATURES.md. |

## Вопросы, которые вероятно задаст жюри

**Почему у этого счёта именно такая роль?** Карточка показывает правило, фактическое значение и
порог, например «9 разных плательщиков, порог 7». Все пороги с единицами и обоснованием — в
`backend/policy.py`; тот же файл читают выгрузки и интерфейс.

**`role_score` — это вероятность?** Нет. Это опора правила: 0,5 ровно на пороге и 1,0 на уровне
насыщения. Размеченных ролей нет, поэтому вероятность здесь не откалибровать.

**Почему транзитов 37, а в подсказке организаторов 72 узла с коэффициентом 0,8–1,2?** Мы
воспроизводим эти 72. Наше правило строже: исходные клиенты исключены, потому что их входящие
занижены, а засчитываются только переводы, отправленные после поступления денег от другого
плательщика.

**Как вы отличаете конечного получателя от обрыва выгрузки?** Исходящие наблюдаются только у счетов
глубины 0–3. Счёт глубины 4 без исходящих никогда не становится «конечным получателем». Для
остальных нужно, чтобы после поступления 90% суммы прошло не меньше 7 дней.

**Нет ли зашитых ответов?** В коде нет списков `gid`: роли, кластеры и приоритеты вычисляются
правилами из `policy.py`. Повторный запуск и перемешанные строки входа дают одинаковые выгрузки,
это проверяет тест.

**Почему Louvain, если переводы направленные?** Louvain ищет плотные группы на взвешенном
неориентированном графе, а направление мы сохраняем там, где оно важно: в ролях, цепочках и на
карте. Зерно фиксировано, поэтому кластеры воспроизводимы.

**Почему в очереди есть исходные клиенты?** Приоритет не исключает исходных клиентов: счёт,
связанный переводами с несколькими другими исходными клиентами, может оказаться узлом выше по
цепочке. Признак «исходный клиент» виден в обосновании и карточке.

**Что изменится при миллионе счетов?** Колоночная обработка вместо словарей Python, Leiden вместо
Louvain, индекс транзакций по отправителю и дате, загрузка в интерфейс только нужной окрестности.
Подробно — README, раздел 10.

**Где персональные данные?** Их нет: только синтетические `gid`, суммы и даты. Всё работает
локально и не отправляет данные наружу.
